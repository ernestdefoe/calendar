/**
 * Admin entry. Everything (scalar settings, permissions, and the custom category
 * manager) is registered declaratively in ./extend — the Flarum 2 way. No
 * imperative initializer is needed.
 */
export { default as extend } from './extend';
